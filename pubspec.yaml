name: flutter_pin_example
description: "Example project for pin and pin_ui Flutter packages"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  pin: ^0.1.1
  pin_ui: ^0.0.1
  flutter_bloc_side_effect: ^0.0.2
  go_router: ^14.3.0
  hidden_logo: ^0.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
